import * as $reflection from "../../_reflection"

export let methodSignature = $reflection.root.lookup(".google.api.methodSignature")

export let defaultHost = $reflection.root.lookup(".google.api.defaultHost")

export let oauthScopes = $reflection.root.lookup(".google.api.oauthScopes")